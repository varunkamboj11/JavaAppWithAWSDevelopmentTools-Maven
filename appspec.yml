version: 0.0
os: linux
files:
  - source: ./my-app.war
    destination: /usr/local/apache-tomcat-9.0.37/webapps
hooks:
  ApplicationStop:
    - location: stop_server.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: start_server.sh
      timeout: 300
      runas: root
